pipeline {
    agent any

    options {
        timestamps()
        overrideIndexTrigger(false)
    }

    stages {
        stage('Build') {
            steps {
                echo 'Hello, World 2'
            }
        }
    }
}